# ActiveGraph fork-and-diff demonstration

Parent `run_dd_activegraph_import_v1` was forked as `run_dd_activegraph_reverse_link_fork_v1` at a quiescent event. The deterministic fixture removed one Lab-to-study reverse relation.

The reverse-link audit produced 1 new finding(s), while ActiveGraph reported 1 divergent relation(s) and 1 divergent object(s). Replay verification was `true`. The scientific source checkout was not changed.
